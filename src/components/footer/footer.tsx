import { Paper } from "@mui/material";
import React from "react";
import { SToolbar } from "./footer.styles";

export const Footer = () => {
  return (
      <SToolbar>
        <div>Player</div>
      </SToolbar>
  );
};
